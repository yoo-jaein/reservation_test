
	<div class="limiter">
		<div class="container-login100">
			
			<div class="jumbotron" style="margin-top: 50px; padding:50px; width:500px; margin-left: auto; margin-right: auto; background-color: white; border-radius: 1; box-shadow: 0 6px 12px rgba(0,0,0,.1);">

          <div class="card-content">
                   <div align="center"><img src="/assets/knu_mark.png"></div>
            <span class="login100-form-title">Member Login</span>
            <hr>
             <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="p-2 field">
              <i class="fa fa-paper-plane"></i>
            <%= f.email_field :email, autocomplete: "email", placeholder: "Email" %><br />
          </div>
        
          <div class="p-2 field">
            <i class="fa fa-lock"></i>
            <%= f.password_field :password, autocomplete: "current-password", placeholder: "Password" %>
          </div>
        
          <% if devise_mapping.rememberable? -%>
            <div class="p-2 field">
              <%= f.check_box :remember_me %>
              Remember
            </div>
          <% end -%>
        
          <div class="container-login100-form-btn p-2 actions">
            <button type="submit" name="commit" class="login100-form-btn">Login</button>
          </div>
          <div class="container-login100-form-btn p-2 actions">
            <button type="submit" name="commit" class="login100-form-btn" href="">administrator -></button>
          </div>
          <% end %>
        
        <%= render "devise/shared/links" %>
              
          </div>
        </div>
				
				</div>
				</div>
	